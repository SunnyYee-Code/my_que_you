# 雀友聚 V1 需求文档

## 1. 文档目标

本文件用于沉淀 `my_que_you` 在 V1 阶段**已经实现并可被代码验证**的需求范围，仅保留已落地能力，不记录未实现或仅规划中的内容，确保 V1 文档与当前项目真实交付范围一致。

---

## 2. V1 产品目标

V1 的目标是完成同城麻将约局的最小可用业务闭环，让用户可以完成：

1. 注册登录并完成基础资料设置；
2. 进入指定城市浏览附近拼团；
3. 发起拼团或申请加入拼团；
4. 房主审核成员申请并管理拼团；
5. 成员通过群聊、私信、通知完成沟通；
6. 对局后通过评价、信用、举报形成基本治理闭环。

---

## 3. 用户角色

| 角色 | 说明 | 当前状态 |
| --- | --- | --- |
| 游客 | 可访问首页、登录注册页和部分浏览入口 | ✅ 已支持 |
| 注册用户 | 可创建拼团、申请入局、聊天、评价、加好友 | ✅ 已支持 |
| 房主 | 发起拼团并审核成员申请，管理拼团成员 | ✅ 已支持 |
| 管理员 | 处理举报、申诉、城市和玩法等后台数据 | ✅ 已支持 |

---

## 3.1 旧 SRS 映射关系

为确保旧《需求规格说明书》删除后仍能追溯需求承接关系，以下列出本次 review 要求必须保留的映射证据：

| 旧 SRS 条目 | 当前承接位置 | 承接口径 |
| --- | --- | --- |
| 状态锁定 | `4.4 入局申请与房主审核 > 状态锁定` | 由 `useJoinGroup.ts` 在申请加入前校验是否已加入、是否存在待审核记录、是否本人房主、是否存在参与中的拼团等条件，阻止重复申请和非法状态流转。 |
| 排他性原则 | `4.3 拼团创建与管理 > 房主约束`、`4.4 入局申请与房主审核 > 状态锁定` | 旧 SRS 中“同一用户同一时间只能处于一个未结束拼团/申请流程”的约束，当前由创建前“存在未结束拼团/参与中拼团则禁止继续发起”和加入前“已加入/待审核/房主身份拦截”共同承接，落点分别在 `GroupCreate.tsx`、`useJoinGroup.ts`。 |
| 性能要求 | 本文档 `6. V1 性能承接口径` | V1 不新增虚假的已达成性能承诺，但保留原 SRS 的性能验证目标与验收说明，作为当前版本验收与后续优化基线。 |

## 4. V1 已实现需求范围

### 4.1 账户与身份体系

| 需求项 | 当前实现说明 | 对应页面/模块 |
| --- | --- | --- |
| 注册登录 | 支持邮箱/手机号/用户名 + 密码登录，支持验证码注册/验证流程，已覆盖邮箱验证注册与手机号登录 | `Login.tsx`、`AuthContext.tsx` |
| 新用户引导页 | 新用户首次进入需完成头像与用户名设置 | `Onboarding.tsx` |
| 个人名片与资料编辑 | 支持编辑昵称、头像，展示信用分、历史拼团与个人主页信息 | `Profile.tsx`、`ProfileEdit.tsx`、`Settings.tsx` |
| 城市定位 | 支持自动定位、城市切换、默认城市记忆与本地缓存 | `CityContext.tsx`、`useGeolocation.ts` |
| 权限区分 | 已区分普通用户、管理员、超管权限 | `AuthContext.tsx`、`Forbidden.tsx` |
| 信用分申诉 | 用户可在设置页提交申诉 | `Settings.tsx` |

### 4.2 拼团发现与浏览

| 需求项 | 当前实现说明 | 对应页面/模块 |
| --- | --- | --- |
| 首页品牌展示 | 首页已提供产品介绍、核心动作入口与附近对局展示 | `Home.tsx` |
| 社区拼团列表 | 支持浏览当前城市拼团列表 | `Index.tsx` |
| 城市隔离 | 列表按当前选定城市展示拼团 | `Index.tsx`、`CityContext.tsx` |
| 距离筛选 | 支持 1/3/5/10/20km 距离过滤 | `Index.tsx`、`useGeolocation.ts` |
| 筛选过滤 | 可按拼团状态过滤，并结合距离条件筛选 | `Index.tsx` |
| 展示策略 | 按开始时间、距离、缺口、玩法、房主信用分等信息进行列表展示 | `Index.tsx`、`StatusBadge.tsx`、`CreditBadge.tsx` |
| 拼团详情查看 | 登录用户可查看拼团详情、成员信息与操作入口 | `GroupDetail.tsx` |

### 4.3 拼团创建与管理

| 需求项 | 当前实现说明 | 对应页面/模块 |
| --- | --- | --- |
| 创建页面字段 | 支持填写时间、地点、人数、玩法、备注等核心字段 | `GroupCreate.tsx` |
| 地图选点 | 已集成地图位置选择能力 | `AmapLocationPicker.tsx`、`GroupCreate.tsx` |
| 房主约束 | 若存在未结束拼团/参与中的拼团，会限制重复发起 | `GroupCreate.tsx`、`useJoinGroup.ts` |
| 每日创建限制 | 调用后端限制每日创建次数 | `GroupCreate.tsx` |
| 拼团取消 | 房主可取消拼团，并触发相应扣分/通知逻辑 | `GroupDetail.tsx` |
| 创建内容安全校验 | 创建拼团时已进行违禁词校验 | `GroupCreate.tsx`、`banned-words.ts` |
| 我的拼团管理 | 支持区分我发起的、我参加的、被移出的拼团 | `MyGroups.tsx` |

### 4.4 入局申请与房主审核

| 需求项 | 当前实现说明 | 对应页面/模块 |
| --- | --- | --- |
| 申请加入拼团 | 用户可在详情页发起申请 | `GroupDetail.tsx`、`useJoinGroup.ts` |
| 状态锁定 | 已校验是否已加入、是否待审核、是否本人房主等，并对核心状态进行锁定校验 | `useJoinGroup.ts` |
| 房主审核 | 房主可查看申请并执行同意/拒绝 | `HostRequests.tsx` |
| 加入成功 | 通过申请后加入成员列表并同步状态 | `useGroups.ts` / RPC |
| 主动退出拼团 | 成员可退出拼团 | `GroupDetail.tsx` |
| 房主移出成员 | 房主可踢人并填写理由 | `GroupDetail.tsx` |
| 退出/移出记录 | 已在个人“我的拼团”及后台保留相关记录视图 | `MyGroups.tsx`、`Admin.tsx` |

### 4.5 消息沟通与通知

| 需求项 | 当前实现说明 | 对应页面/模块 |
| --- | --- | --- |
| 临时聊天室 | 群内支持实时文本聊天 | `Chat.tsx`、`useMessages.ts` |
| 私信聊天 | 支持好友/用户间一对一私聊 | `DirectChat.tsx`、`useDirectMessages.ts` |
| 消息通知中心 | 可查看系统通知并跳转到关联页面 | `Notifications.tsx`、`useNotifications.ts` |
| 实时通知更新 | 已通过订阅方式接收实时通知/消息更新 | `useNotifications.ts`、`useMessages.ts`、`useDirectMessages.ts` |
| 房主邀请好友入局 | 支持向好友发送拼团邀请 | `InviteFriendsDialog.tsx`、`useGroupInvitations.ts` |
| 一键导航 | 拼团详情支持导航到地点 | `GroupDetail.tsx` |
| 聊天违禁词校验 | 群聊与私信发送前已有违禁词校验 | `banned-words.ts`、`Chat.tsx`、`DirectChat.tsx` |

### 4.6 社交关系与信任机制

| 需求项 | 当前实现说明 | 对应页面/模块 |
| --- | --- | --- |
| 好友系统 | 支持好友搜索、申请、接受、拒绝、删除 | `Friends.tsx`、`useFriends.ts` |
| 局后社交沉淀 | 已具备社交关系沉淀能力，结合私信/好友体系可持续联系 | `Friends.tsx`、`DirectChat.tsx` |
| 用户主页查看 | 可查看他人主页、信用分、历史拼团与评价摘要 | `Profile.tsx` |
| 信用分限制 | 在列表、主页、详情等关键位置展示，并在低信用场景触发限制逻辑 | `CreditBadge.tsx`、多页面 |
| 互评机制 | 支持对局后多维度评价，并限制评价时效 | `Review.tsx` |
| 自动结束与信用奖惩 | 已支持手动结束后的评价与信用分流转，完成一局加分、异常退出扣分 | `GroupDetail.tsx`、`Review.tsx`、相关信用逻辑 |
| 违规举报 | 支持发起举报并进入后台处理 | `ReportDialog.tsx`、`Admin.tsx` |

### 4.7 平台治理与后台

| 需求项 | 当前实现说明 | 对应页面/模块 |
| --- | --- | --- |
| 管理后台 | 已具备独立后台页面与多标签管理结构 | `Admin.tsx` |
| 用户管理 | 后台可查看用户信息与角色相关数据 | `Admin.tsx` |
| 拼团管理 | 后台可查看拼团及相关运行数据 | `Admin.tsx` |
| 城市管理 | 支持新增/删除城市 | `Admin.tsx` |
| 玩法管理 | 可查看玩法数据 | `Admin.tsx` |
| 举报处理 | 后台可查看举报记录 | `Admin.tsx` |
| 评价巡检 | 后台可查看评价记录 | `Admin.tsx` |
| 申诉处理 | 后台可查看申诉记录 | `Admin.tsx` |
| 退出记录追踪 | 后台可查看退出/移出记录 | `Admin.tsx` |
| 聊天巡检 | 后台可查看聊天相关数据 | `Admin.tsx` |
| 违禁词过滤 | 后台已提供系统设置和违禁词管理面板，聊天与创建流程接入违禁词过滤 | `Admin.tsx`、`banned-words.ts` |

---

## 5. V1 核心业务闭环

### 5.1 用户找局闭环

1. 用户注册/登录；
2. 首次进入完成新手引导；
3. 选择城市并授权定位；
4. 浏览附近拼团列表并筛选；
5. 查看拼团详情；
6. 提交入局申请；
7. 房主审核通过后加入拼团；
8. 进入群聊沟通并按时到场；
9. 对局结束后完成评价。

### 5.2 房主开局闭环

1. 房主创建拼团；
2. 系统校验是否允许开团；
3. 拼团展示到社区列表；
4. 房主接收申请通知；
5. 房主审核成员；
6. 成局后成员进入聊天；
7. 如有异常可取消拼团或移出成员；
8. 对局结束后沉淀评价与信用。

---

## 6. 文档回补说明

本文件依据以下事实来源整理：

- `memory-bank/需求规格说明书.md` 中已确认且已实现的需求描述；
- `memory-bank/产品优先级.md` 中与 V1 已交付范围一致的能力定义；
- `src/pages`、`src/hooks`、`src/contexts`、`src/components` 中的实际实现情况；
- 当前仓库路由与页面能力。

本文件仅描述 V1 当前已经落地的真实范围，未实现能力统一迁移到 `memory-bank/v2` 文档中管理。

---

## 6. V1 性能承接口径

旧 SRS 中的性能要求并未因为文档拆分而失效，V1 继续沿用以下**性能验证目标/验收说明**作为承接口径：

| 原 SRS 性能要求 | V1 承接口径 | 验收说明 |
| --- | --- | --- |
| 首页首屏加载时间：4G 网络下 ≤ 3 秒 | 作为线上体验目标保留，V1 验收时需在接近真实移动网络条件下做首屏加载抽样验证 | 若未达标，不得口头删除要求，应在验收结论中记录实际结果、瓶颈位置和是否阻塞发布。 |
| 列表分页加载响应时间：P95 ≤ 800ms | 作为列表浏览链路的性能基线保留 | 需通过接口日志、浏览器面板或压测记录给出证据，确认分页加载在目标范围内或明确偏差。 |
| 核心接口（登录、创建拼团、申请加入）成功率 ≥ 99.5% | 作为核心业务链路稳定性目标保留 | 提测前至少需要具备可统计成功率的日志/监控口径；若暂未形成正式看板，也要在测试或灰度阶段保留统计方法。 |

说明：这里的“承接”不是简单写成“后续做性能优化”，而是明确保留原 SRS 的性能目标、验证对象和验收方式，确保旧文档删除后仍可追溯。


## 7. 2026-03-25 自动化验收回写

本轮仅回写**已经补齐并实际跑通**的自动化场景，不将未执行的需求条目标记为完成。

### 7.1 已补齐并通过的 Vitest 需求承接

- 账户与身份：补齐手机号登录识别。
- 城市与找局：补齐城市切换刷新、定位失败提示、无坐标拼团距离排序置底。
- 创建与管理：补齐创建时长上限校验、必填项缺失禁止发布、房主取消拼团。
- 详情与社交：补齐详情页关键信息展示、私信邀请卡片展示、局后多维评价提交。
- 后台治理：补齐城市新增删除、举报/评价/申诉/退出记录 tab 数据展示、违禁词新增后缓存失效与前台新词拦截。

### 7.2 已补齐并通过的 Playwright 需求承接

- 首页品牌与核心入口可见。
- 登录页协议勾选门禁可见，登录按钮默认禁用。
- 登录页注册入口文案可见。
- 社区页发现控件与距离筛选入口可见。
- 未登录访问创建页、评价页、后台时会跳转登录页。
- 未知路由 404 回退页可见。

### 7.3 当前口径

- 上述条目可在 V1 范围内标记为**自动化已完成**。
- 仍依赖真实账号、Supabase 在线数据、地图定位或多端实时联动的完整业务闭环，不在本轮标记为已完成。
