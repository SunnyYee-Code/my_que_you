# 雀友聚 V2 测试用例

## 1. 说明

本文档基于 `memory-bank/v2/需求文档.md` 与 `memory-bank/v2/任务执行计划.md` 输出可执行测试用例设计，按 V2 任务包逐步补充。

当前已补充 **T9 账号合规与身份增强** 下两项能力的测试用例设计与当前验证结果：

- `4.1.1 账号注销完整闭环`
- `4.1.2 实名认证（前后端能力与自动化验证）`

当前已补充 `T10 拼团效率与履约增强` 下以下能力的测试用例设计与当前验证结果：

- `4.2.1 紧急补位`
- `4.2.2 推荐排序`
- `4.2.3 开局提醒`

当前已补充 `T11 社交安全与信任增强` 下以下能力的测试用例设计与当前验证结果：

- `4.3.1 黑名单屏蔽`
- `4.3.2 标签化评价与履约档案增强`

测试用例按测试层级拆分为：

- 单元测试（Unit）
- 集成测试（Integration）
- 真实场景测试 / 人工测试（Manual / Real Scenario）

为便于快速查看，本文统一使用以下状态标记：

- ✅ 已完成：已有自动化或本地验证证据，且当前分支已执行通过
- ❌ 未完成：尚未形成可验证证据
- 🟡 需真实场景测试（V2统一执行）：必须依赖真实账号、真实 Supabase、真实调度、真实通知、多端登录态或完整业务环境，当前仅做测试设计与特殊标识，待 **V2 全部开发完成后统一执行**

---

## 2. T9 账号合规与身份增强

### 2.1 4.1.1 账号注销完整闭环

#### 2.1.1 单元测试设计

### 2.1.1.1 建议覆盖模块（含当前完成状态）

| 用例ID      | 模块             | 用例名称                             | 状态      | 原因/备注                                                        | 前置条件                                            | 步骤                                | 预期结果                                                      |
| ----------- | ---------------- | ------------------------------------ | --------- | ---------------------------------------------------------------- | --------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------- |
| UT-V2-T9-01 | 账号注销状态快照 | 未申请状态返回可申请动作             | ✅ 已完成 | 已由 `src/lib/__tests__/account-deletion.test.ts` 覆盖           | 构造 `not_applied` 快照数据                         | 调用 `buildAccountDeletionSnapshot` | 返回 `canOperate=true`、`availableActions=['apply']`          |
| UT-V2-T9-02 | 注销申请资格校验 | 存在进行中牌局时禁止申请注销         | ✅ 已完成 | 已由 `src/lib/__tests__/account-deletion.test.ts` 覆盖           | `hasActiveGroups=true`                              | 调用 `validateApplyEligibility`     | 返回 `ok=false` 且错误为 `ACTIVE_GROUPS`                      |
| UT-V2-T9-03 | 撤销资格校验     | 仅冷静期状态允许撤销注销申请         | ✅ 已完成 | 已由 `src/lib/__tests__/account-deletion.test.ts` 覆盖           | 分别构造 `cooling_off` / `completed`                | 调用 `validateCancelEligibility`    | `cooling_off` 可撤销，`completed` 不可撤销                    |
| UT-V2-T9-04 | 冷静期计算       | 默认冷静期到期时间为 15 天后         | ✅ 已完成 | 已由 `src/lib/__tests__/account-deletion.test.ts` 覆盖           | 固定当前时间                                        | 调用 `calculateCoolingOffExpireAt`  | 返回 15 天后的 ISO 时间                                       |
| UT-V2-T9-05 | 自动完成判定     | 冷静期已到期时应进入自动完成流程     | ✅ 已完成 | 已由 `src/lib/__tests__/account-deletion-processor.test.ts` 覆盖 | `status=cooling_off` 且 `coolingOffExpireAt <= now` | 调用 `shouldFinalizeDeletion`       | 返回 `true`                                                   |
| UT-V2-T9-06 | 自动完成拦截     | 非冷静期或已删除记录不可重复自动完成 | ✅ 已完成 | 已由 `src/lib/__tests__/account-deletion-processor.test.ts` 覆盖 | `status=cancelled` 或 `deletedAt!=null`             | 调用 `shouldFinalizeDeletion`       | 返回 `false`                                                  |
| UT-V2-T9-07 | 数据脱敏补丁     | 自动完成时生成账号禁用与资料脱敏补丁 | ✅ 已完成 | 已由 `src/lib/__tests__/account-deletion-processor.test.ts` 覆盖 | 传入 `userId` 与 `deletedAt`                        | 调用 `buildFinalizeDeletionPatch`   | 返回 profile / auth 更新补丁，包含禁用、脱敏与 completed 状态 |

#### 2.1.1.2 单元测试建议落地文件（含完成状态）

| 建议文件                                               | 覆盖内容                                     | 状态      | 原因/备注        |
| ------------------------------------------------------ | -------------------------------------------- | --------- | ---------------- |
| `src/lib/__tests__/account-deletion.test.ts`           | 注销申请资格、撤销资格、冷静期计算、状态快照 | ✅ 已完成 | 已存在并执行通过 |
| `src/lib/__tests__/account-deletion-processor.test.ts` | 自动完成判定、脱敏补丁生成                   | ✅ 已完成 | 已存在并执行通过 |

---

#### 2.1.2 集成测试设计

### 2.1.2.1 页面/模块集成用例（含当前完成状态）

| 用例ID      | 模块                           | 用例名称                                     | 状态      | 原因/备注                                                              | 前置条件                                 | 步骤                                             | 预期结果                                                                                                                   |
| ----------- | ------------------------------ | -------------------------------------------- | --------- | ---------------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| IT-V2-T9-01 | Settings                       | 用户从未申请状态提交注销申请                 | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖                      | `applyStatus=not_applied`、允许操作      | 点击“确认注销”                                   | 调用申请接口并提示“注销申请已提交”                                                                                         |
| IT-V2-T9-02 | Settings                       | 不满足条件时展示禁用原因并阻止申请           | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖                      | `canOperate=false`、带 `forbiddenReason` | 打开设置页                                       | 按钮禁用并展示后端原因                                                                                                     |
| IT-V2-T9-03 | Settings                       | 冷静期状态下展示说明并支持撤销申请           | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖                      | `applyStatus=cooling_off`                | 点击“撤销注销申请”                               | 调用撤销接口并提示“已撤销注销申请”                                                                                         |
| IT-V2-T9-04 | Settings                       | completed 最终态仅展示结果，不再暴露操作入口 | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖                      | `applyStatus=completed`                  | 打开设置页                                       | 展示最终态文案与 `resultReason`，且不显示撤销/确认注销按钮                                                                 |
| IT-V2-T9-05 | Settings + Auth                | 注销完成后当前登录态被强制退出并跳转登录页   | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖                      | `applyStatus=completed`                  | 打开设置页并等待副作用执行                       | 调用 `signOut()`、跳转 `/login`、提示“账号已注销”                                                                          |
| IT-V2-T9-06 | Account Deletion Edge Function | 查询注销状态接口返回统一状态字段             | ✅ 已完成 | 当前已由后端代码与本地测试辅助验证；真实环境待后续场景测试             | 准备状态查询请求                         | 调用 `GET /functions/v1/account-deletion/status` | 返回 `applyStatus`、`canOperate`、`forbiddenReason`、`coolingOffExpireAt`、`resultReason`、`updatedAt`、`availableActions` |
| IT-V2-T9-07 | Auto Finalize Processor        | 冷静期到期后自动完成注销任务执行数据更新     | ✅ 已完成 | 当前已由处理器逻辑与迁移脚本、本地测试辅助验证；真实环境待后续场景测试 | 构造到期注销记录                         | 执行自动完成处理逻辑                             | 注销单状态改为 `completed`，同时生成 profile/auth/通知/审计补丁                                                            |

### 2.1.2.2 本轮已验证命令与结果

| 验证项                | 命令                                                                                                                                                      | 结果                                        |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| T9 账号注销自动化测试 | `npm test -- --run src/lib/__tests__/account-deletion.test.ts src/lib/__tests__/account-deletion-processor.test.ts src/pages/__tests__/Settings.test.tsx` | ✅ 已完成，3 个测试文件通过，15/15 用例通过 |
| 前端构建验证          | `npm run build`                                                                                                                                           | ✅ 已完成，build 通过                       |

---

#### 2.1.3 真实场景测试 / 人工测试设计

> 说明：以下用例**必须依赖真实账号、真实 Supabase 环境、真实调度或多端会话**，当前阶段只做测试设计与特殊标识；待 **V2 全部开发完成后统一执行测试**。

| 用例ID      | 模块             | 用例名称                                           | 状态                            | 原因/备注                                            | 前置条件                                    | 步骤                                                  | 预期结果                                                                                                |
| ----------- | ---------------- | -------------------------------------------------- | ------------------------------- | ---------------------------------------------------- | ------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| RT-V2-T9-01 | 注销申请真实入库 | 用户发起注销后真实写入注销申请记录                 | 🟡 需真实场景测试（V2统一执行） | 需真实 Supabase 数据库验证写库结果                   | 准备真实测试账号，账号无进行中牌局/结算阻塞 | 在设置页发起注销申请                                  | `account_deletion_requests` 新增记录，`profiles.deletion_status` 更新为 `cooling_off`，前端展示申请成功 |
| RT-V2-T9-02 | 冷静期真实撤销   | 冷静期内撤销申请后真实恢复账号状态                 | 🟡 需真实场景测试（V2统一执行） | 需真实数据库与状态回写验证                           | 账号已进入冷静期                            | 点击“撤销注销申请”                                    | 注销单状态更新为 `cancelled`，前端恢复正常状态，账号权限恢复                                            |
| RT-V2-T9-03 | 自动调度         | 冷静期到期后 `pg_cron` 真实触发自动完成            | 🟡 需真实场景测试（V2统一执行） | 依赖真实 `pg_cron` 与 `net.http_post` 调度能力       | 准备已到期冷静期记录                        | 等待调度或手动触发真实任务                            | `auto-finalize-account-deletion` 被真实调用，注销单进入 `completed`                                     |
| RT-V2-T9-04 | 账号禁用生效     | 注销完成后真实账号无法继续正常使用                 | 🟡 需真实场景测试（V2统一执行） | 依赖真实 Auth 用户禁用能力                           | 账号已完成注销                              | 使用原账号重新访问受保护页面或重新登录                | 登录失败或受保护接口/页面被拦截                                                                         |
| RT-V2-T9-05 | 资料脱敏落库     | 注销完成后 profile / deleted_emails 脱敏与落库正确 | 🟡 需真实场景测试（V2统一执行） | 需真实数据库校验                                     | 账号已完成注销                              | 检查 `profiles`、`deleted_emails`                     | 昵称改为“已注销用户”，头像/手机号/城市等字段脱敏，删除邮箱记录存在                                      |
| RT-V2-T9-06 | 通知与审计       | 注销关键节点通知和审计真实落库                     | 🟡 需真实场景测试（V2统一执行） | 依赖真实通知与审计表                                 | 分别执行申请、撤销、自动完成链路            | 检查 `notifications` 与 `account_deletion_audit_logs` | 关键节点均有对应通知和审计记录，action 正确                                                             |
| RT-V2-T9-07 | 登录态收敛       | 注销完成后当前端真实会话被强制退出                 | 🟡 需真实场景测试（V2统一执行） | 自动化仅覆盖前端副作用，真实会话仍需验证             | 当前账号保持登录，后台已完成注销            | 刷新页面或重新请求用户态                              | 前端提示“账号已注销”，并跳转登录页                                                                      |
| RT-V2-T9-08 | 多端一致性       | 多端同时登录时，注销完成后其他端也能感知并退出     | 🟡 需真实场景测试（V2统一执行） | 需多端 / 多浏览器真实会话验证                        | 同一账号同时在两个端登录                    | 一端完成注销或等待自动完成                            | 其他端在合理时机被拦截、退出或刷新为已注销状态                                                          |
| RT-V2-T9-09 | 核心链路兼容性   | 注销流程不影响 V1 已有登录、拼团、消息等核心链路   | 🟡 需真实场景测试（V2统一执行） | 需在更完整的 V2 版本上统一回归，避免单项测试结论失真 | V2 全量开发完成                             | 执行账号注销前后相关核心链路回归                      | 未注销用户链路正常；已注销用户被正确限制，不出现脏状态                                                  |

---

### 2.2 4.1.2 实名认证

#### 2.2.1 单元测试设计

### 2.2.1.1 建议覆盖模块（含当前完成状态）

| 用例ID         | 模块               | 用例名称                                    | 状态      | 原因/备注                                                         | 前置条件                                            | 步骤                                   | 预期结果                                                |
| -------------- | ------------------ | ------------------------------------------- | --------- | ----------------------------------------------------------------- | --------------------------------------------------- | -------------------------------------- | ------------------------------------------------------- |
| UT-V2-T9-RN-01 | 状态归一           | approved / cancelled 状态归一为前台统一口径 | ✅ 已完成 | 已由 `src/lib/__tests__/real-name-verification.test.ts` 覆盖      | 构造 `approved` / `cancelled` 快照                  | 调用状态归一与 viewModel 构建          | 前台状态统一映射为 `verified` / `unverified`            |
| UT-V2-T9-RN-02 | 展示文案兜底       | 后端展示文案为空时回退到默认状态文案        | ✅ 已完成 | 已由 `src/lib/__tests__/real-name-verification.test.ts` 覆盖      | `display_status_text=''`                            | 调用 `getRealNameStatusViewModel`      | 返回默认状态文案，如 `认证失败`                         |
| UT-V2-T9-RN-03 | 资格校验           | 提交 / 重提 / 撤销资格与状态机保持一致      | ✅ 已完成 | 已由 `src/lib/__tests__/real-name-function-contract.test.ts` 覆盖 | 分别构造 `unverified` / `pending` / `rejected` 状态 | 调用资格校验函数                       | 仅允许对应状态触发提交、重提或撤销                      |
| UT-V2-T9-RN-04 | 限制场景与兼容     | 默认限制场景、旧枚举别名与未知场景均不放行  | ✅ 已完成 | 已由 `src/lib/__tests__/real-name-verification.test.ts` 覆盖      | 构造默认快照、legacy 场景值与未知场景值             | 调用 `shouldShowRealNameGuard`         | legacy 场景可兼容识别；未知场景在受限级别下 fail-closed |
| UT-V2-T9-RN-05 | 快照契约           | 统一状态快照字段满足前台/后端契约预期       | ✅ 已完成 | 已由 `src/lib/__tests__/real-name-function-contract.test.ts` 覆盖 | 构造 approved 状态机输入                            | 调用 `buildRealNameStatusSnapshot`     | 返回统一字段，包括状态、可操作性、限制级别、时间字段    |
| UT-V2-T9-RN-06 | 敏感信息与风险标记 | 身份证脱敏与风险标记生成正确                | ✅ 已完成 | 已由 `src/lib/__tests__/real-name-function-contract.test.ts` 覆盖 | 传入身份证号与风险输入                              | 调用 `maskIdNumber` / `buildRiskFlags` | 返回脱敏后的身份证号与预期风控 flags                    |

### 2.2.1.2 单元测试建议落地文件（含完成状态）

| 建议文件                                                | 覆盖内容                                                        | 状态      | 原因/备注        |
| ------------------------------------------------------- | --------------------------------------------------------------- | --------- | ---------------- |
| `src/lib/__tests__/real-name-verification.test.ts`      | 状态归一、文案兜底、限制场景判断、legacy 兼容、fail-closed 判定 | ✅ 已完成 | 已存在并执行通过 |
| `src/lib/__tests__/real-name-function-contract.test.ts` | 资格校验、统一快照契约、敏感信息脱敏、风险标记                  | ✅ 已完成 | 已存在并执行通过 |

---

#### 2.2.2 集成测试设计

### 2.2.2.1 页面/模块集成用例（含当前完成状态）

| 用例ID         | 模块        | 用例名称                                    | 状态      | 原因/备注                                            | 前置条件                                                           | 步骤                       | 预期结果                                                       |
| -------------- | ----------- | ------------------------------------------- | --------- | ---------------------------------------------------- | ------------------------------------------------------------------ | -------------------------- | -------------------------------------------------------------- |
| IT-V2-T9-RN-01 | Settings    | 未实名用户在设置页提交实名认证              | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖    | `status=unverified`、填写真实姓名/身份证号并勾选授权               | 点击“提交认证”             | 调用提交接口并提示“实名认证提交成功”                           |
| IT-V2-T9-RN-02 | Settings    | 审核中状态展示并支持撤销申请                | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖    | `status=pending`、`can_cancel=true`                                | 打开设置页并点击“撤销申请” | 展示审核中状态，调用撤销接口并提示“已撤销实名认证申请”         |
| IT-V2-T9-RN-03 | Settings    | 已实名 / 认证失败状态按统一适配器正确展示   | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖    | 分别构造 `approved`、`rejected` 快照                               | 打开设置页                 | 已实名状态正确展示；认证失败时展示驳回原因并出现“重新提交”按钮 |
| IT-V2-T9-RN-04 | Settings    | cancelled 状态回退到未实名动作，不发生崩溃  | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖    | `status=cancelled`                                                 | 打开设置页                 | 页面正常渲染，展示未实名后的提交动作                           |
| IT-V2-T9-RN-05 | GroupCreate | 创建拼团场景命中实名限制时展示 guard 并阻断 | ✅ 已完成 | 已由 `src/pages/__tests__/GroupCreate.test.tsx` 覆盖 | `restriction_level=limited`、`restriction_scenes=['group_create']` | 打开创建页并补全表单       | 展示实名限制提示，发布按钮保持禁用，可跳转至设置页实名认证入口 |
| IT-V2-T9-RN-06 | GroupCreate | 实名查询异常时创建拼团场景默认不放行        | ✅ 已完成 | 已由 `src/pages/__tests__/GroupCreate.test.tsx` 覆盖 | 实名查询报错 / 快照缺失                                            | 打开创建页                 | 仍展示实名限制提示，发布按钮禁用，前台 fail-closed             |
| IT-V2-T9-RN-07 | GroupDetail | 申请加入场景命中实名限制时展示 guard 并阻断 | ✅ 已完成 | 已由 `src/pages/__tests__/GroupDetail.test.tsx` 覆盖 | `restriction_level=limited`、`restriction_scenes=['group_join']`   | 打开详情页                 | 展示实名限制提示，申请加入按钮禁用                             |
| IT-V2-T9-RN-08 | GroupDetail | 实名查询异常时申请加入场景默认不放行        | ✅ 已完成 | 已由 `src/pages/__tests__/GroupDetail.test.tsx` 覆盖 | 实名查询报错 / 快照缺失                                            | 打开详情页                 | 仍展示实名限制提示，申请加入按钮禁用，前台 fail-closed         |

### 2.2.2.2 本轮已验证命令与结果

| 验证项                | 命令                                                                                                                                                                                                                                                                                                                                               | 结果                                        |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| T9 实名认证自动化测试 | `npm test -- --run src/lib/__tests__/real-name-verification.test.ts src/lib/__tests__/real-name-function-contract.test.ts src/lib/__tests__/real-name-backend.test.ts src/lib/__tests__/real-name-function-backend-contract.test.ts src/pages/__tests__/Settings.test.tsx src/pages/__tests__/GroupCreate.test.tsx src/pages/__tests__/GroupDetail.test.tsx` | ✅ 已完成，7 个测试文件通过，46/46 用例通过 |
| T9 全量自动化回归     | `npm test -- --run src/lib/__tests__/account-deletion.test.ts src/lib/__tests__/account-deletion-processor.test.ts src/lib/__tests__/real-name-verification.test.ts src/lib/__tests__/real-name-function-contract.test.ts src/lib/__tests__/real-name-backend.test.ts src/lib/__tests__/real-name-function-backend-contract.test.ts src/pages/__tests__/Settings.test.tsx src/pages/__tests__/GroupCreate.test.tsx src/pages/__tests__/GroupDetail.test.tsx` | ✅ 已完成，9 个测试文件通过，53/53 用例通过 |
| 前端构建验证          | `npm run build`                                                                                                                                                                                                                                                                                                                                    | ✅ 已完成，build 通过                       |

---

#### 2.2.3 真实场景测试 / 人工测试设计

> 说明：以下用例必须依赖真实 Supabase 数据表 / 真实审核流 / 真实风控规则 / 真实前后端联调环境，当前阶段仅做测试设计与特殊标识；待 T9 后端能力补齐或 V2 联调阶段统一执行。

| 用例ID         | 模块 / 场景      | 用例名称                                                  | 状态                            | 原因/备注                                      | 前置条件                     | 步骤                                 | 预期结果                                                |
| -------------- | ---------------- | --------------------------------------------------------- | ------------------------------- | ---------------------------------------------- | ---------------------------- | ------------------------------------ | ------------------------------------------------------- |
| RT-V2-T9-RN-01 | 实名提交真实写库 | 用户提交实名认证后真实写入认证记录                        | 🟡 需真实场景测试（V2统一执行） | 当前仓库已补齐 migration / function，仍需真实部署验证 | 准备真实测试账号             | 在设置页填写信息并提交               | 认证记录成功入库，状态按低风险自动通过或进入 `pending` 待审核态 |
| RT-V2-T9-RN-02 | 审核通过回写     | 审核通过后状态、时间与展示文案真实回写                    | 🟡 需真实场景测试（V2统一执行） | 需真实审核流 / 回写机制                        | 后台或脚本将认证单置为通过   | 刷新设置页 / 相关页面                | 设置页展示已实名，通过时间正确；创建/加入限制解除       |
| RT-V2-T9-RN-03 | 审核驳回回写     | 审核驳回后失败原因在前台真实展示                          | 🟡 需真实场景测试（V2统一执行） | 需真实审核流 / 驳回原因字段确认                | 后台或脚本将认证单置为驳回   | 刷新设置页                           | 展示认证失败与驳回原因，可重新提交                      |
| RT-V2-T9-RN-04 | 风控口径联动     | restriction_level / restriction_scenes 与关键场景口径一致 | 🟡 需真实场景测试（V2统一执行） | 需真实风控规则与限制口径                       | 为账号配置不同限制等级与场景 | 分别访问创建页与详情页申请入口       | 仅在目标场景展示提示并阻断；非目标场景不误伤            |
| RT-V2-T9-RN-05 | 隐私与敏感字段   | 实名撤销/驳回后敏感字段处理符合合规要求                   | 🟡 需真实场景测试（V2统一执行） | 需确认数据库字段保留/脱敏/清理策略             | 已提交实名认证资料           | 执行撤销或驳回后检查数据库与前台展示 | 敏感字段处理符合约定，不出现明文泄漏或脏数据展示        |
| RT-V2-T9-RN-06 | 服务端强制拦截   | 绕过前台直接调用创建/申请接口时仍受实名限制               | 🟡 需真实场景测试（V2统一执行） | 自动化当前主要覆盖前台行为，需确认真实环境中的服务端强校验 / RLS 防绕过 | 准备未实名测试账号           | 直接请求相关接口或绕过前台发起操作   | 服务端仍拒绝未实名账号创建拼团或申请加入                |
| RT-V2-T9-RN-07 | 鉴权与路由       | 前端调用实名认证 Edge Function 时真实携带登录态并成功鉴权 | 🟡 需真实场景测试（V2统一执行） | 当前代码已补 `Authorization` header，仍需线上环境验证 | 当前账号已登录               | 打开设置页执行状态查询 / 提交 / 撤销 | `/functions/v1/real-name-verification/*` 返回真实数据，不出现 401 |
| RT-V2-T9-RN-08 | 管理审核闭环     | 管理员真实审核通过/驳回后状态、通知与审计回写正确         | 🟡 需真实场景测试（V2统一执行） | 需真实管理员角色与数据库验证                  | 准备 `pending` 状态认证申请  | 调用管理员审核接口或后台审核操作     | `profiles`、请求表、通知表、审计表状态一致更新         |
| RT-V2-T9-RN-09 | 加密与合规       | 认证姓名等敏感信息在库内以约定加密格式保存且可满足审计要求 | 🟡 需真实场景测试（V2统一执行） | 当前代码已改为 AES-GCM 加密，仍需确认 secret 配置与合规要求 | Functions secret 已配置      | 提交实名认证并检查落库字段           | `real_name_encrypted` 非可逆明文/简单编码，密钥配置正确，满足内部合规预期 |

---

## 3. T10 拼团效率与履约增强

### 3.1 4.2.1 紧急补位

#### 3.1.1 单元测试设计

### 3.1.1.1 建议覆盖模块（含当前完成状态）

| 用例ID      | 模块         | 用例名称                                       | 状态      | 原因/备注                                                         | 前置条件                                            | 步骤                              | 预期结果                                                         |
| ----------- | ------------ | ---------------------------------------------- | --------- | ----------------------------------------------------------------- | --------------------------------------------------- | --------------------------------- | ---------------------------------------------------------------- |
| UT-V2-T10-01 | 紧急补位规则 | 临近开局且缺少少量名额的开放场次识别为紧急补位 | ✅ 已完成 | 已由 `src/lib/__tests__/group-emergency-fill.test.ts` 覆盖        | `status=OPEN`、`needed_slots<=2`、开局时间在 120 分钟内 | 调用 `getGroupEmergencyFillMeta`  | 返回 `isEmergencyFill=true`、展示 `紧急补位` 与立即补位动作      |
| UT-V2-T10-02 | 紧急补位规则 | 开局时间过远的场次不进入紧急补位               | ✅ 已完成 | 已由 `src/lib/__tests__/group-emergency-fill.test.ts` 覆盖        | `status=OPEN`、开局时间超过 120 分钟                | 调用 `getGroupEmergencyFillMeta`  | 返回 `isEmergencyFill=false`                                     |
| UT-V2-T10-03 | 紧急补位规则 | 已关闭或已过期开局的场次不进入紧急补位         | ✅ 已完成 | 已由 `src/lib/__tests__/group-emergency-fill.test.ts` 覆盖        | `status=FULL` 或 `start_time < now`                 | 调用 `getGroupEmergencyFillMeta`  | 返回 `isEmergencyFill=false`，避免误把无效场次当作紧急补位       |

### 3.1.1.2 单元测试建议落地文件（含完成状态）

| 建议文件                                        | 覆盖内容                                   | 状态      | 原因/备注        |
| ----------------------------------------------- | ------------------------------------------ | --------- | ---------------- |
| `src/lib/__tests__/group-emergency-fill.test.ts` | 紧急补位识别、倒计时文案、关闭/过期兜底规则 | ✅ 已完成 | 已存在并执行通过 |

---

#### 3.1.2 集成测试设计

### 3.1.2.1 页面/模块集成用例（含当前完成状态）

| 用例ID      | 模块        | 用例名称                                         | 状态      | 原因/备注                                                  | 前置条件                                             | 步骤                                | 预期结果                                                                 |
| ----------- | ----------- | ------------------------------------------------ | --------- | ---------------------------------------------------------- | ---------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------ |
| IT-V2-T10-01 | Index       | 紧急补位场次在列表页优先曝光并展示专属入口       | ✅ 已完成 | 已由 `src/pages/__tests__/Index.test.tsx` 覆盖             | 准备一条紧急补位场次和一条普通开放场次               | 打开列表页                          | 紧急补位场次排序更靠前，并展示 `紧急补位` 标识及 `立即补位` 按钮         |
| IT-V2-T10-02 | GroupDetail | 紧急补位场次在详情页展示横幅与倒计时说明         | ✅ 已完成 | 已由 `src/pages/__tests__/GroupDetail.test.tsx` 覆盖       | 场次临近开局且 `needed_slots > 0`                    | 打开拼团详情页                      | 页面展示 `紧急补位中` 提示及临近开局说明                                 |
| IT-V2-T10-03 | GroupDetail | 成员退出后若仍临近开局且缺人，通知文案带紧急补位 | ✅ 已完成 | 已由 `src/pages/__tests__/GroupDetail.test.tsx` 覆盖       | 场次处于临近开局阶段，成员点击退出                   | 在详情页执行退出拼团                | 更新名额后仍保持 `OPEN`，并向房主写入包含 `紧急补位` 提示的通知内容       |
| IT-V2-T10-04 | Index       | 已加入 / 审核中状态优先于紧急补位按钮文案        | ✅ 已完成 | 已由 `src/pages/__tests__/Index.test.tsx` 覆盖             | 紧急补位场次分别命中 `isMember`、`isPending`         | 打开列表页                          | 按钮分别保持 `已加入`、`审核中`，不会被错误替换成 `立即补位`             |
| IT-V2-T10-05 | Index       | 游客浏览紧急补位场次时仍可走登录前加入引导       | ✅ 已完成 | 已由 `src/pages/__tests__/Index.test.tsx` 覆盖             | 未登录状态下存在紧急补位场次                         | 点击列表页加入按钮                  | 仍展示紧急补位标识，点击按钮后跳转登录页                                 |
| IT-V2-T10-06 | GroupDetail | 补位通知发送失败时不误报整条退出链路失败         | ✅ 已完成 | 已由 `src/pages/__tests__/GroupDetail.test.tsx` 覆盖       | 退出主链路成功，但通知写入失败                       | 在详情页执行退出拼团                | 页面保留成功结果提示，同时额外提示“补位通知发送失败” warning              |
| IT-V2-T10-07 | GroupDetail | 房主移除成员后展示紧急补位提示 toast             | ✅ 已完成 | 已由 `src/pages/__tests__/GroupDetail.test.tsx` 覆盖       | 房主移除成员后场次仍临近开局且出现缺口               | 在详情页执行移除成员                | 页面提示“已触发紧急补位”，并保持后续刷新链路                             |

### 3.1.2.2 本轮已验证命令与结果

| 验证项              | 命令                                                                                                                                                                                  | 结果                                        |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| T10 紧急补位自动化测试 | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/lib/__tests__/group-emergency-fill.test.ts src/pages/__tests__/Index.test.tsx src/pages/__tests__/GroupDetail.test.tsx` | ✅ 已完成，3 个测试文件通过，25/25 用例通过 |

---

#### 3.1.3 真实场景测试 / 人工测试设计

> 说明：以下用例仍依赖真实 Supabase 数据、真实房主/成员账号、多端消息与临近开局真实时间推进，当前阶段仅补充测试设计；待 V2 联调阶段统一执行。

| 用例ID      | 模块 / 场景     | 用例名称                                     | 状态                            | 原因/备注                                            | 前置条件                             | 步骤                                    | 预期结果                                                                 |
| ----------- | --------------- | -------------------------------------------- | ------------------------------- | ---------------------------------------------------- | ------------------------------------ | --------------------------------------- | ------------------------------------------------------------------------ |
| RT-V2-T10-01 | 列表优先曝光    | 临近开局缺人场次在真实列表中优先展示         | 🟡 需真实场景测试（V2统一执行） | 需真实城市数据、真实开局时间和真实列表接口返回顺序   | 准备同城多条开放场次，其中一条满足紧急补位条件 | 打开首页/找局列表并刷新                 | 紧急补位场次优先展示，标识与倒计时文案正确                              |
| RT-V2-T10-02 | 详情页状态提示  | 紧急补位场次在详情页展示实时提示             | 🟡 需真实场景测试（V2统一执行） | 需真实时间推进和真实详情接口数据                     | 场次满足临近开局且存在空位             | 打开拼团详情页                           | 出现 `紧急补位中` 提示，缺口人数与倒计时和真实数据一致                   |
| RT-V2-T10-03 | 状态回收与通知  | 成员退出 / 踢人后真实触发紧急补位提示与通知 | 🟡 需真实场景测试（V2统一执行） | 需真实成员、真实通知表与真实退出链路                 | 准备一场临近开局且接近满员的场次       | 成员退出或房主踢人后刷新详情页与消息中心 | `groups.needed_slots` 更新为最新值，场次保持开放，房主收到带紧急补位提示的消息 |

---

### 3.2 4.2.2 推荐排序

#### 3.2.1 单元测试设计

### 3.2.1.1 建议覆盖模块（含当前完成状态）

| 用例ID      | 模块         | 用例名称                                       | 状态      | 原因/备注                                                     | 前置条件                                                     | 步骤                              | 预期结果                                                                 |
| ----------- | ------------ | ---------------------------------------------- | --------- | ------------------------------------------------------------- | ------------------------------------------------------------ | --------------------------------- | ------------------------------------------------------------------------ |
| UT-V2-T10-04 | 推荐排序规则 | 其他条件相同时，距离更近的开放场次排序更靠前   | ✅ 已完成 | 已由 `src/lib/__tests__/group-recommendation.test.ts` 覆盖    | 准备两条 `OPEN` 场次，开局时间与玩法一致，仅距离不同         | 调用 `sortGroupsByRecommendation` | 距离更近的场次排在前面                                                   |
| UT-V2-T10-05 | 推荐排序规则 | 距离相同时，更早开局的场次排序更靠前           | ✅ 已完成 | 已由 `src/lib/__tests__/group-recommendation.test.ts` 覆盖    | 准备两条 `OPEN` 场次，距离一致但开局时间不同                 | 调用 `sortGroupsByRecommendation` | 更早开局的场次排在前面                                                   |
| UT-V2-T10-06 | 偏好推断     | 用户历史参与/主持过的玩法会被识别为偏好玩法    | ✅ 已完成 | 已由 `src/lib/__tests__/group-recommendation.test.ts` 覆盖    | 用户在历史 `FULL` / `IN_PROGRESS` 场次中出现且带有 `play_style` | 调用 `inferPreferredPlayStyles`   | 返回按频次聚合后的玩法偏好列表                                           |
| UT-V2-T10-07 | 推荐排序规则 | 命中用户偏好玩法的开放场次获得额外推荐加权     | ✅ 已完成 | 已由 `src/lib/__tests__/group-recommendation.test.ts` 覆盖    | 准备玩法匹配和玩法不匹配两条开放场次，其他条件相近           | 调用 `sortGroupsByRecommendation` | 匹配偏好玩法的场次优先于非偏好玩法场次                                   |
| UT-V2-T10-08 | 推荐排序兜底 | 距离/偏好缺失时回退到时间与新鲜度排序，不报错   | ✅ 已完成 | 已由 `src/lib/__tests__/group-recommendation.test.ts` 覆盖    | 场次缺失定位距离，且用户暂无玩法偏好                         | 调用 `sortGroupsByRecommendation` | 仍返回稳定排序结果，不因空值崩溃                                         |
| UT-V2-T10-09 | 推荐排序规则 | 默认推荐排序中，可加入开放场次优先于已满场次   | ✅ 已完成 | 已由 `src/lib/__tests__/group-recommendation.test.ts` 覆盖    | 准备一条 `OPEN` 场次与一条 `FULL` 历史偏好场次               | 调用 `sortGroupsByRecommendation` | `OPEN` 场次优先展示，避免历史偏好满员局压过可加入场次                    |
| UT-V2-T10-10 | 偏好推断     | 当前开放中的实时场次不会被误计入历史玩法偏好   | ✅ 已完成 | 已由 `src/lib/__tests__/group-recommendation.test.ts` 覆盖    | 同时准备 `OPEN` 实时场次与 `COMPLETED` 历史场次             | 调用 `inferPreferredPlayStyles`   | 仅历史场次参与偏好推断，当前实时开放场次不会污染玩法偏好                 |

### 3.2.1.2 单元测试建议落地文件（含完成状态）

| 建议文件                                         | 覆盖内容                                                   | 状态      | 原因/备注        |
| ------------------------------------------------ | ---------------------------------------------------------- | --------- | ---------------- |
| `src/lib/__tests__/group-recommendation.test.ts` | 推荐分数模型、偏好推断、开放场次优先、空值兜底与紧急补位优先 | ✅ 已完成 | 已新增并执行通过 |

---

#### 3.2.2 集成测试设计

### 3.2.2.1 页面/模块集成用例（含当前完成状态）

| 用例ID      | 模块  | 用例名称                                           | 状态      | 原因/备注                                              | 前置条件                                                 | 步骤             | 预期结果                                                                 |
| ----------- | ----- | -------------------------------------------------- | --------- | ------------------------------------------------------ | -------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------ |
| IT-V2-T10-08 | Index | 列表默认使用智能推荐排序，偏好玩法场次排序更靠前   | ✅ 已完成 | 已由 `src/pages/__tests__/Index.test.tsx` 覆盖         | 用户有历史偏好玩法，准备一条偏好玩法场次和一条非偏好场次 | 打开找局列表页   | 默认排序下，偏好玩法场次展示在非偏好玩法场次之前                         |
| IT-V2-T10-09 | Index | 智能推荐排序下，开放场次优先于已满历史场次         | ✅ 已完成 | 已由 `src/pages/__tests__/Index.test.tsx` 直接覆盖     | 用户有历史偏好满员场次，同时存在可加入开放场次           | 打开找局列表页   | 可加入 `OPEN` 场次优先展示，历史 `FULL` 场次不会压过当前可申请场次       |
| IT-V2-T10-10 | Index | 手动切换距离最近排序时仍保持无坐标场次置底兜底     | ✅ 已完成 | 已由 `src/pages/__tests__/Index.test.tsx` 既有用例承接 | 列表中同时存在有坐标场次和无坐标场次                     | 切换排序为距离最近 | 有坐标场次按距离排序，无坐标场次保持在列表底部，推荐排序改造不破坏既有行为 |
| IT-V2-T10-11 | Home  | 首页默认智能推荐会主动请求定位以接入距离因子       | ✅ 已完成 | 已由 `src/pages/__tests__/Home.test.tsx` 覆盖          | 首页存在可预览场次，用户尚未获得定位                     | 打开首页         | 页面会请求定位，确保智能推荐可接入真实距离数据                           |
| IT-V2-T10-12 | Home  | 首页预览列表会复用历史玩法偏好提升匹配场次曝光     | ✅ 已完成 | 已由 `src/pages/__tests__/Home.test.tsx` 覆盖          | 用户存在历史偏好玩法，同时首页有偏好/非偏好开放场次      | 打开首页         | 偏好玩法场次在预览列表中优先展示                                         |
| IT-V2-T10-13 | Home  | 首页定位失败后不会自动重复触发定位请求             | ✅ 已完成 | 已由 `src/pages/__tests__/Home.test.tsx` 覆盖          | 首页存在可预览场次，`useGeolocation` 已返回错误信息      | 打开首页         | 页面不会在已有定位错误时继续自动重试，避免重复触发定位请求               |

### 3.2.2.2 本轮已验证命令与结果

| 验证项                | 命令                                                                                                                                                                                                                           | 结果                                           |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| T10 推荐排序自动化测试 | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/lib/__tests__/group-recommendation.test.ts src/pages/__tests__/Index.test.tsx`                                                               | ✅ 已完成，2 个测试文件通过，16/16 用例通过     |
| T10 推荐排序扩展测试   | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/lib/__tests__/group-recommendation.test.ts src/pages/__tests__/Index.test.tsx src/pages/__tests__/Home.test.tsx`                            | ✅ 已完成，3 个测试文件通过，21/21 用例通过     |
| T10 已完成范围回归     | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/lib/__tests__/group-emergency-fill.test.ts src/lib/__tests__/group-recommendation.test.ts src/pages/__tests__/Index.test.tsx src/pages/__tests__/Home.test.tsx src/pages/__tests__/GroupDetail.test.tsx` | ✅ 已完成，5 个测试文件通过，37/37 用例通过     |
| 前端构建验证           | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm build`                                                                                                                                                             | ✅ 已完成，构建通过；输出包含 Browserslist 数据过旧提示、`banned-words.ts` 动静态混用 warning 与 chunk size warning |

---

#### 3.2.3 真实场景测试 / 人工测试设计

> 说明：以下用例仍依赖真实定位、真实同城场次、真实用户偏好沉淀与线上行为指标，当前阶段仅补充测试设计；待 V2 联调阶段统一执行。

| 用例ID      | 模块 / 场景 | 用例名称                                           | 状态                            | 原因/备注                                            | 前置条件                                                     | 步骤                               | 预期结果                                                                 |
| ----------- | ----------- | -------------------------------------------------- | ------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------ | ---------------------------------- | ------------------------------------------------------------------------ |
| RT-V2-T10-04 | 首页 / 找局 | 同城列表默认按智能推荐规则返回更高相关性的场次     | 🟡 需真实场景测试（V2统一执行） | 需真实定位、真实时间推进与真实用户历史玩法偏好数据   | 账号有明确历史玩法偏好，准备多条不同距离/时间/玩法的开放场次 | 打开首页与找局列表并刷新           | 两个入口排序口径一致，偏好玩法且距离/时间更优的场次优先展示             |
| RT-V2-T10-05 | 找局列表    | 缺失定位或偏好数据时，列表仍能稳定回退到时间兜底排序 | 🟡 需真实场景测试（V2统一执行） | 需验证真实定位关闭、首次登录或偏好样本不足的情况     | 关闭定位，使用无历史玩法偏好的账号                           | 打开找局列表并切换智能推荐         | 列表正常展示，不报错；排序回退为可解释的时间/新鲜度顺序                 |
| RT-V2-T10-06 | 数据验证    | 推荐排序上线后可对点击率 / 申请率进行对比追踪      | 🟡 需真实场景测试（V2统一执行） | 当前本地环境无法形成真实曝光、点击与申请转化数据闭环 | 准备真实埋点/日志采集环境                                     | 上线后观察推荐排序相关指标         | 可区分推荐排序前后或不同排序模式下的点击率、申请率，并形成验收对比口径 |

---

### 3.3 4.2.3 开局提醒

#### 3.3.1 单元测试设计

### 3.3.1.1 建议覆盖模块（含当前完成状态）

| 用例ID       | 模块         | 用例名称                                     | 状态      | 原因/备注                                                        | 前置条件                                                               | 步骤                                | 预期结果                                                                 |
| ------------ | ------------ | -------------------------------------------- | --------- | ---------------------------------------------------------------- | ---------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------ |
| UT-V2-T10-11 | 提醒计划规则 | 房主与成员提醒对象会去重生成                 | ✅ 已完成 | 已由 `src/lib/__tests__/group-start-reminder.test.ts` 覆盖       | 成员列表中同时包含房主、重复成员                                        | 调用 `buildGroupStartReminderPlans` | 仅生成 1 条房主提醒与去重后的成员提醒，不会对房主重复按成员身份发送       |
| UT-V2-T10-12 | 提醒计划规则 | 已取消场次不会继续生成开局提醒计划           | ✅ 已完成 | 已由 `src/lib/__tests__/group-start-reminder.test.ts` 覆盖       | `status=CANCELLED`                                                     | 调用 `buildGroupStartReminderPlans` | 返回空数组，避免已取消场次继续排入提醒                                   |
| UT-V2-T10-13 | 提醒状态流转 | 开局时间变更后旧提醒记录会被标记为 superseded | ✅ 已完成 | 已由 `src/lib/__tests__/group-start-reminder.test.ts` 覆盖       | 已存在旧 `scheduled_start_time`，最新 `start_time` 已变化              | 调用 `resolveGroupStartReminderStatus` | 返回 `superseded`，提示旧提醒计划失效                                     |
| UT-V2-T10-14 | 提醒状态流转 | 已取消场次对应的提醒记录会被标记为 cancelled | ✅ 已完成 | 已由 `src/lib/__tests__/group-start-reminder.test.ts` 覆盖       | 提醒记录当前为 `pending`，最新场次状态为 `CANCELLED`                   | 调用 `resolveGroupStartReminderStatus` | 返回 `cancelled`，避免继续发送                                             |
| UT-V2-T10-15 | 发送时机判定 | 仅在 remind_at 到达后且开局前窗口内发送提醒  | ✅ 已完成 | 已由 `src/lib/__tests__/group-start-reminder.test.ts` 覆盖       | 提醒计划的 `remind_at=开局前30分钟`                                     | 调用 `shouldSendGroupStartReminder` | 早于 `remind_at` 不发送；达到 `remind_at` 开始发送；开局后不再发送         |
| UT-V2-T10-16 | 通知文案构建 | 房主与成员提醒文案按角色区分并附带正确跳转   | ✅ 已完成 | 已由 `src/lib/__tests__/group-start-reminder.test.ts` 覆盖       | 分别构造房主与成员提醒上下文                                            | 调用 `buildGroupStartReminderNotification` | 房主文案包含成团缺口提示；成员文案强调准时到场；`linkTo=/group/:id`       |
| UT-V2-T10-17 | 发送编排     | 提醒已被其他调度实例 claim 时不会重复写入通知 | ✅ 已完成 | 已由 `src/lib/__tests__/group-start-reminder-delivery.test.ts` 覆盖 | 已存在待发送提醒记录，但 `claimForSending` 未成功                        | 调用 `deliverGroupStartReminder`    | 返回 `already_claimed`，且不会再次调用通知发送                            |
| UT-V2-T10-18 | 发送编排     | 通知已写入但 sent 回写失败时不会回退为 failed | ✅ 已完成 | 已由 `src/lib/__tests__/group-start-reminder-delivery.test.ts` 覆盖 | 通知发送成功，但 `markSent` 返回失败                                     | 调用 `deliverGroupStartReminder`    | 返回 `mark_sent_failed`，且不会执行 `markFailed` 导致后续重复发送          |

### 3.3.1.2 单元测试建议落地文件（含完成状态）

| 建议文件                                           | 覆盖内容                                                         | 状态      | 原因/备注        |
| -------------------------------------------------- | ---------------------------------------------------------------- | --------- | ---------------- |
| `src/lib/__tests__/group-start-reminder.test.ts`   | 提醒计划去重、取消拦截、时间变更作废、发送窗口、房主/成员文案差异 | ✅ 已完成 | 已新增并执行通过 |
| `src/lib/__tests__/group-start-reminder-delivery.test.ts` | 发送 claim、防重发、sent 回写失败保护                             | ✅ 已完成 | 已新增并执行通过 |

---

#### 3.3.2 集成测试设计

### 3.3.2.1 页面/模块集成用例（含当前完成状态）

| 用例ID       | 模块                 | 用例名称                                       | 状态      | 原因/备注                                                  | 前置条件                                                     | 步骤                   | 预期结果                                                                 |
| ------------ | -------------------- | ---------------------------------------------- | --------- | ---------------------------------------------------------- | ------------------------------------------------------------ | ---------------------- | ------------------------------------------------------------------------ |
| IT-V2-T10-14 | Reminder Edge Function | 开局前 30 分钟内为房主和成员生成站内提醒并落库 | ❌ 未完成 | 当前仅完成规则单测与实现落地，尚无针对 Edge Function 的自动化验证证据 | 存在 `OPEN/FULL` 且 `start_time` 落入未来 30 分钟窗口的场次 | 执行 `auto-send-group-start-reminders` | 为房主与成员写入 `notifications`，并在 `group_start_reminders` 记录 `sent` |
| IT-V2-T10-15 | Reminder Edge Function | 开局时间变更后旧提醒计划被 superseded，不重复发送 | ❌ 未完成 | 当前仅由状态流转单测覆盖规则，尚无函数级集成测试证据      | 已存在旧 `scheduled_start_time` 的提醒记录，场次 `start_time` 已更新 | 再次执行提醒任务         | 旧记录更新为 `superseded`，仅按最新开局时间继续调度                       |
| IT-V2-T10-16 | Reminder Edge Function | 已取消场次的待发送提醒被标记为 cancelled       | ❌ 未完成 | 当前仅由状态流转单测覆盖规则，尚无函数级集成测试证据      | 场次状态更新为 `CANCELLED`                                   | 执行提醒任务             | 待发送记录更新为 `cancelled`，不会继续写入提醒通知                        |
| IT-V2-T10-17 | Notifications         | 消息中心基础展示与点击链路支持开局提醒通知     | ✅ 已完成 | 已由 `src/pages/__tests__/Notifications.test.tsx` 回归覆盖基础展示、已读与跳转行为 | 准备一条 `group_start_reminder` 类型通知                       | 打开消息中心并点击通知   | 提醒通知标题/内容可见，点击后仍会标记已读并跳转目标页面                   |

### 3.3.2.2 本轮已验证命令与结果

| 验证项                | 命令                                                                                                                                                                                                                                                                                           | 结果                                                                 |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| T10 开局提醒自动化测试 | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/lib/__tests__/group-start-reminder.test.ts src/lib/__tests__/group-start-reminder-delivery.test.ts src/pages/__tests__/Notifications.test.tsx`                                                                                                      | ✅ 已完成，3 个测试文件通过，12/12 用例通过；覆盖规则、防重发编排与消息中心基础交互回归 |
| T10 全量范围回归       | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/lib/__tests__/group-emergency-fill.test.ts src/lib/__tests__/group-recommendation.test.ts src/lib/__tests__/group-start-reminder.test.ts src/lib/__tests__/group-start-reminder-delivery.test.ts src/pages/__tests__/Index.test.tsx src/pages/__tests__/Home.test.tsx src/pages/__tests__/GroupDetail.test.tsx src/pages/__tests__/Notifications.test.tsx` | ✅ 已完成，8 个测试文件通过，49/49 用例通过；仅存在既有 React Router / Dialog warning |
| 前端构建验证           | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm build`                                                                                                                                                                                                                             | ✅ 已完成，构建通过；输出包含 Browserslist 数据过旧提示、`banned-words.ts` 动静态混用 warning 与 chunk size warning |

---

#### 3.3.3 真实场景测试 / 人工测试设计

> 说明：以下用例仍依赖真实 Supabase migration、真实 Edge Function 定时调度、真实多账号消息收件箱与真实开局时间推进，当前阶段仅补充测试设计；待 V2 联调阶段统一执行。

| 用例ID       | 模块 / 场景     | 用例名称                                           | 状态                            | 原因/备注                                                     | 前置条件                                                           | 步骤                                           | 预期结果                                                                 |
| ------------ | --------------- | -------------------------------------------------- | ------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------ | ---------------------------------------------- | ------------------------------------------------------------------------ |
| RT-V2-T10-07 | 定时提醒调度    | 开局前 30 分钟房主和成员收到正确提醒               | 🟡 需真实场景测试（V2统一执行） | 依赖真实定时任务、真实账号、真实通知表与真实站内消息收件箱     | 准备 1 个房主账号、2 个成员账号与一场 30 分钟后开始的 `OPEN/FULL` 场次 | 等待调度窗口并检查各账号消息中心                 | 房主与成员收到不同文案的提醒通知，落库对象准确、无重复发送               |
| RT-V2-T10-08 | 开局时间变更    | 开局时间调整后旧提醒失效并按新时间重新发送         | 🟡 需真实场景测试（V2统一执行） | 依赖真实 migration 落库和多轮调度执行                         | 先生成一条待发送提醒，再修改 `groups.start_time`                    | 观察 `group_start_reminders` 状态并等待新窗口     | 旧提醒记录变为 `superseded`，新时间点到达后仅发送一轮新提醒               |
| RT-V2-T10-09 | 取消场次拦截    | 已取消场次不会继续发送开局提醒                     | 🟡 需真实场景测试（V2统一执行） | 需验证真实取消链路与提醒任务执行顺序                         | 准备一条接近提醒窗口的场次，并在触发前将状态改为 `CANCELLED`         | 执行提醒任务并检查提醒表/通知表                   | 待发送提醒更新为 `cancelled`，`notifications` 不新增开局提醒              |
| RT-V2-T10-10 | 多角色文案校验  | 房主与成员的提醒内容、跳转链接和消息中心展示一致   | 🟡 需真实场景测试（V2统一执行） | 需真实消息中心展示与多账号点击跳转验证                       | 房主与成员均已收到开局提醒                                          | 分别登录房主/成员账号查看消息中心并点击通知        | 房主看到成团/缺口提醒，成员看到准时到场提醒；点击跳转到正确的拼团详情页    |
| RT-V2-T10-11 | 调度入口保护    | 未鉴权定时提醒函数仅允许受控调度入口触发           | 🟡 需真实场景测试（V2统一执行） | 当前 `auto-send-group-start-reminders` 配置为 `verify_jwt = false`，需补充部署侧保护验证 | 已完成真实环境部署，具备调度平台或受控调用入口                     | 校验部署配置并尝试从非预期入口触发                 | 仅受控调度来源可触发任务；非预期外部调用被限制、拒绝或无敏感影响           |

---

## 4. T11 社交安全与信任增强

### 4.1 4.3.1 黑名单屏蔽

#### 4.1.1 单元测试设计

### 4.1.1.1 建议覆盖模块（含当前完成状态）

| 用例ID       | 模块         | 用例名称                                           | 状态      | 原因/备注                                                | 前置条件                                      | 步骤                              | 预期结果                                                                       |
| ------------ | ------------ | -------------------------------------------------- | --------- | -------------------------------------------------------- | --------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------ |
| UT-V2-T11-01 | 黑名单状态归一 | 双方无黑名单关系时返回可互动状态                   | ✅ 已完成 | 已由 `src/lib/__tests__/user-blacklist.test.ts` 覆盖     | 不传黑名单记录                                | 调用 `getUserBlacklistState`      | 返回 `isBlocked=false`、`relationship='none'`、`reason=''`                     |
| UT-V2-T11-02 | 黑名单状态归一 | 当前用户拉黑对方时返回 `blocked_by_me`             | ✅ 已完成 | 已由 `src/lib/__tests__/user-blacklist.test.ts` 覆盖     | 准备一条 `blocker_id=currentUser` 的记录      | 调用 `getUserBlacklistState`      | 返回 `isBlocked=true`、`relationship='blocked_by_me'` 与对应提示文案           |
| UT-V2-T11-03 | 黑名单状态归一 | 对方拉黑当前用户时返回 `blocked_by_them`           | ✅ 已完成 | 已由 `src/lib/__tests__/user-blacklist.test.ts` 覆盖     | 准备一条 `blocker_id=targetUser` 的记录       | 调用 `getUserBlacklistState`      | 返回 `isBlocked=true`、`relationship='blocked_by_them'` 与对应提示文案         |
| UT-V2-T11-04 | 黑名单 hook   | 加入黑名单时同步清理双方既有好友 / 待处理关系      | ✅ 已完成 | 已由 `src/hooks/__tests__/useBlacklist.test.tsx` 覆盖    | 当前用户已登录，目标用户存在                  | 调用 `useAddToBlacklist().mutateAsync` | 先写入 `user_blacklist`，再删除双方 `friendships` 关系并触发相关查询失效        |

### 4.1.1.2 单元测试建议落地文件（含完成状态）

| 建议文件                                        | 覆盖内容                                         | 状态      | 原因/备注        |
| ----------------------------------------------- | ------------------------------------------------ | --------- | ---------------- |
| `src/lib/__tests__/user-blacklist.test.ts`      | 黑名单关系归一、双向提示文案                     | ✅ 已完成 | 已新增并执行通过 |
| `src/hooks/__tests__/useBlacklist.test.tsx`     | 黑名单写入后的好友关系清理与 mutation 行为校验   | ✅ 已完成 | 已新增并执行通过 |

---

#### 4.1.2 集成测试设计

### 4.1.2.1 页面/模块集成用例（含当前完成状态）

| 用例ID       | 模块        | 用例名称                                           | 状态      | 原因/备注                                              | 前置条件                                   | 步骤                           | 预期结果                                                                 |
| ------------ | ----------- | -------------------------------------------------- | --------- | ------------------------------------------------------ | ------------------------------------------ | ------------------------------ | ------------------------------------------------------------------------ |
| IT-V2-T11-01 | Profile     | 在他人主页执行拉黑                                 | ✅ 已完成 | 已由 `src/pages/__tests__/Profile.test.tsx` 覆盖       | 当前登录用户访问他人主页                    | 点击“拉黑用户”                 | 调用拉黑能力并提示“已加入黑名单”，进入隔离状态                           |
| IT-V2-T11-02 | Settings    | 设置页展示黑名单管理并支持移除                     | ✅ 已完成 | 已由 `src/pages/__tests__/Settings.test.tsx` 覆盖      | 当前用户黑名单中存在成员                    | 打开设置页并点击“移除黑名单”   | 页面展示黑名单成员，调用移除能力并提示“已移除黑名单”                     |
| IT-V2-T11-03 | DirectChat  | 私聊命中黑名单时展示隔离提示并禁止继续发送         | ✅ 已完成 | 已由 `src/pages/__tests__/DirectChat.test.tsx` 覆盖    | 当前会话命中黑名单关系                      | 打开私聊页                     | 展示黑名单提示文案，消息输入框禁用，发送按钮不可用                       |
| IT-V2-T11-04 | Friends     | 黑名单用户在好友搜索场景下不再暴露“加好友”操作     | ✅ 已完成 | 已由 `src/pages/__tests__/Friends.test.tsx` 覆盖       | 搜索结果命中 `blocked_by_me` 或 `blocked_by_them` | 在好友页按 UID 搜索用户         | 页面展示 `已拉黑` / `对方已屏蔽你` 状态，不再出现“加好友”入口             |

### 4.1.2.2 本轮已验证命令与结果

| 验证项                | 命令                                                                                                                                                                                                                                                                                        | 结果                                                                 |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| T11 黑名单自动化测试  | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/hooks/__tests__/useBlacklist.test.tsx src/lib/__tests__/user-blacklist.test.ts src/pages/__tests__/Profile.test.tsx src/pages/__tests__/DirectChat.test.tsx src/pages/__tests__/Settings.test.tsx` | ✅ 已完成，5 个测试文件通过，22/22 用例通过；覆盖规则、hook、资料页、私聊页、设置页 |
| T11 受影响范围回归    | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/hooks/__tests__/useBlacklist.test.tsx src/lib/__tests__/user-blacklist.test.ts src/pages/__tests__/Profile.test.tsx src/pages/__tests__/DirectChat.test.tsx src/pages/__tests__/Settings.test.tsx src/pages/__tests__/Notifications.test.tsx src/pages/__tests__/Friends.test.tsx` | ✅ 已完成，7 个测试文件通过，29/29 用例通过；仅存在既有 React Router / Dialog warning |
| 前端构建验证          | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vite build`                                                                                                                                                                                                                | ✅ 已完成，构建通过；输出包含 Browserslist 数据过旧提示、`banned-words.ts` 动静态混用 warning 与 chunk size warning |

---

#### 4.1.3 真实场景测试 / 人工测试设计

> 说明：以下用例仍依赖真实 Supabase migration、真实 RLS / trigger、生效中的多账号关系数据与真实页面登录态，当前阶段仅补充测试设计；待 V2 联调阶段统一执行。

| 用例ID       | 模块 / 场景       | 用例名称                                         | 状态                            | 原因/备注                                                 | 前置条件                                             | 步骤                                       | 预期结果                                                                 |
| ------------ | ----------------- | ------------------------------------------------ | ------------------------------- | --------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------ |
| RT-V2-T11-01 | 黑名单真实写库    | 用户拉黑他人后真实写入 `user_blacklist`          | 🟡 需真实场景测试（V2统一执行） | 需真实 Supabase 数据库验证写库结果                       | 准备两个真实测试账号，双方当前未互相拉黑             | 在资料页点击“拉黑用户”                     | `user_blacklist` 新增记录，前端展示成功提示                              |
| RT-V2-T11-02 | 好友关系清理      | 拉黑后真实清理双方既有好友 / 待处理请求关系      | 🟡 需真实场景测试（V2统一执行） | 需验证 migration 触发器与真实数据联动                    | 两账号之间预先存在 `accepted` 或 `pending` 关系      | 一方执行拉黑后刷新好友页 / 好友请求页       | 双方原有 `friendships` 关系被删除，前端不再展示该好友或待处理请求        |
| RT-V2-T11-03 | 私聊写入拦截      | 命中黑名单后真实数据库拒绝新增私聊消息           | 🟡 需真实场景测试（V2统一执行） | 自动化当前主要覆盖前台隔离，仍需验证 trigger 防绕过       | 两账号已建立黑名单关系                               | 尝试通过前台或绕过前台直接写入 `direct_messages` | 写入失败并返回预期错误，前台保持隔离提示                                 |
| RT-V2-T11-04 | 双向隔离口径      | 对方拉黑当前用户时，当前用户也会被前台隔离       | 🟡 需真实场景测试（V2统一执行） | 需真实双账号切换与双向状态查询验证                       | A 账号拉黑 B 账号                                    | 使用 B 账号访问 A 的私聊 / 资料页            | B 账号看到“由于对方的隐私设置，当前无法继续互动”等双向隔离提示          |
| RT-V2-T11-05 | 取消拉黑恢复      | 取消拉黑后资料页、设置页、私聊链路恢复正常       | 🟡 需真实场景测试（V2统一执行） | 自动化当前已覆盖移除动作，但仍需验证真实数据回流和页面恢复 | 两账号间已有黑名单关系                               | 设置页移除黑名单后重新进入资料页 / 私聊页     | 黑名单记录被移除，前台恢复可互动状态，后续私聊写入恢复正常               |
| RT-V2-T11-06 | 约束与异常提示    | 重复拉黑、RLS 拒绝或异常返回时前台提示符合预期   | 🟡 需真实场景测试（V2统一执行） | 需验证唯一索引冲突、RLS 与真实错误文案                    | 准备重复拉黑 / 非法操作等异常场景                    | 执行重复拉黑或越权操作                        | 前台收到稳定可理解的失败提示，不出现脏状态                               |

---

### 4.2 4.3.2 标签化评价与履约档案增强

#### 4.2.1 单元测试设计

### 4.2.1.1 建议覆盖模块（含当前完成状态）

| 用例ID       | 模块             | 用例名称                                       | 状态      | 原因/备注                                              | 前置条件                                            | 步骤                               | 预期结果                                                                 |
| ------------ | ---------------- | ---------------------------------------------- | --------- | ------------------------------------------------------ | --------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------ |
| UT-V2-T11-05 | 评价标签归一     | 非法标签会被过滤，重复标签会被去重             | ✅ 已完成 | 已由 `src/lib/__tests__/review-insights.test.ts` 覆盖  | 准备包含重复值与非法值的标签数组                    | 调用 `normalizeReviewTags`         | 仅保留合法标签，顺序稳定，最多返回 3 个标签                             |
| UT-V2-T11-06 | 履约档案汇总     | 基于评价标签、已完成场次与失约记录生成履约率和高频标签 | ✅ 已完成 | 已由 `src/lib/__tests__/review-insights.test.ts` 覆盖  | 准备 `reviews`、`groups` 与 `group_member_exits` 历史数据 | 调用 `buildFulfillmentProfile`     | 返回 `completedCount`、`breachCount`、`fulfillmentRate` 与高频标签摘要 |

### 4.2.1.2 单元测试建议落地文件（含完成状态）

| 建议文件                                        | 覆盖内容                                   | 状态      | 原因/备注        |
| ----------------------------------------------- | ------------------------------------------ | --------- | ---------------- |
| `src/lib/__tests__/review-insights.test.ts`     | 标签归一、履约档案汇总、高频标签排序       | ✅ 已新增并执行通过 | 已完成 |

---

#### 4.2.2 集成测试设计

### 4.2.2.1 页面/模块集成用例（含当前完成状态）

| 用例ID       | 模块         | 用例名称                                           | 状态      | 原因/备注                                              | 前置条件                                          | 步骤                           | 预期结果                                                                 |
| ------------ | ------------ | -------------------------------------------------- | --------- | ------------------------------------------------------ | ------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------ |
| IT-V2-T11-05 | Review       | 用户提交评价时可同时提交结构化标签                 | ✅ 已完成 | 已由 `src/pages/__tests__/Review.test.tsx` 覆盖       | 当前用户可对已结束牌局的其他成员发起评价          | 勾选标签并提交评价             | `reviews` 写入包含 `tags` 字段，前台提示“评价已提交”                     |
| IT-V2-T11-06 | Profile      | 用户主页展示履约率、完成场次、失约记录与高频标签    | ✅ 已完成 | 已由 `src/pages/__tests__/Profile.test.tsx` 覆盖      | 目标用户存在历史评价标签、已完成场次与失约记录数据  | 打开用户主页                   | 页面展示“履约档案”、履约率、高频好评标签与风险提示；无历史样本时展示“暂无记录” |
| IT-V2-T11-07 | HostRequests | 房主审核申请时展示申请人的履约率与正负向标签摘要     | ✅ 已完成 | 已由 `src/pages/__tests__/HostRequests.test.tsx` 覆盖 | 存在待审核申请，申请人已有历史牌局、评价标签或失约记录 | 打开审核管理页                 | 页面展示申请人的履约率、已完成场次、高频好评标签与风险标签，辅助审核判断 |

### 4.2.2.2 本轮已验证命令与结果

| 验证项                      | 命令                                                                                                                                                                                                                                                                                                                                 | 结果                                                                 |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- |
| T11 标签评价与履约档案回归  | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm exec vitest run src/hooks/__tests__/useBlacklist.test.tsx src/lib/__tests__/user-blacklist.test.ts src/lib/__tests__/review-insights.test.ts src/pages/__tests__/Profile.test.tsx src/pages/__tests__/DirectChat.test.tsx src/pages/__tests__/Settings.test.tsx src/pages/__tests__/Friends.test.tsx src/pages/__tests__/HostRequests.test.tsx src/pages/__tests__/Review.test.tsx` | ✅ 已完成，9 个测试文件通过，38/38 用例通过；覆盖黑名单既有能力与 T11 新增能力 |
| 前端构建验证                | `export PATH=/usr/local/bin:/opt/homebrew/bin:$PATH && pnpm build`                                                                                                                                                                                                                                                                  | ✅ 已完成，构建通过；输出包含既有 Browserslist 与 chunk warning，不影响产物生成 |

---

#### 4.2.3 真实场景测试 / 人工测试设计

> 说明：以下用例仍依赖真实 Supabase migration、真实评价写库、多账号历史牌局数据与真实审核链路，当前阶段仅补充测试设计；待 V2 联调阶段统一执行。

| 用例ID       | 模块 / 场景     | 用例名称                                             | 状态                            | 原因/备注                                              | 前置条件                                              | 步骤                                   | 预期结果                                                                 |
| ------------ | --------------- | ---------------------------------------------------- | ------------------------------- | ------------------------------------------------------ | ----------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------ |
| RT-V2-T11-07 | 评价真实写库    | 提交评价时标签字段真实写入 `reviews.tags`            | 🟡 需真实场景测试（V2统一执行） | 需验证 migration 已部署且线上写库兼容                 | 准备一场已结束且可评价的真实牌局                       | 选择多个标签并提交评价                 | `reviews.tags` 成功写入标签数组，前台提示成功                           |
| RT-V2-T11-08 | 资料页真实展示  | 用户主页基于真实历史数据展示履约率与高频标签摘要     | 🟡 需真实场景测试（V2统一执行） | 需验证真实历史评价、真实完成场次、真实失约记录与展示口径一致 | 目标用户已有多条带标签评价、已完成场次与失约记录数据 | 打开资料页并刷新                        | 履约率、完成场次、失约记录与标签摘要和真实历史数据一致                 |
| RT-V2-T11-09 | 审核辅助决策    | 房主审核申请时真实展示申请人的履约档案摘要           | 🟡 需真实场景测试（V2统一执行） | 需验证多账号真实 join request 与履约摘要查询链路       | 房主账号存在待审核申请，申请人有历史拼团、评价与失约记录 | 打开审核管理页                          | 房主可看到申请人的履约率、已完成场次、高频好评标签与风险标签            |
| RT-V2-T11-10 | 存量兼容        | 老评价记录无标签时，资料页与审核页仍可稳定展示摘要   | 🟡 需真实场景测试（V2统一执行） | 需验证历史无 `tags` 数据与新字段兼容                   | 目标用户仅存在历史无标签评价或混合新旧评价数据          | 打开资料页 / 审核页                     | 页面正常展示，不因空标签报错；履约率等非标签摘要仍可正常计算            |

---

## 5. 当前阶段测试结论（针对 T9 + T10 + T11 当前已开发能力）

### 5.1 代码提测门槛

- 结论：**有条件通过**
  - 依据：
  - T9 账号注销 + 实名认证相关自动化测试已通过（53/53）
  - T10 紧急补位 + 推荐排序 + 开局提醒相关自动化测试已通过（49/49）
  - T11 黑名单屏蔽 + 标签化评价与履约档案相关自动化测试已通过（38/38）
  - 前端构建通过
  - T9 范围内当前已形成的本地自动化证据：
    - 账号注销：`src/lib/__tests__/account-deletion.test.ts`
    - 账号注销：`src/lib/__tests__/account-deletion-processor.test.ts`
    - 实名认证：`src/lib/__tests__/real-name-verification.test.ts`
    - 实名认证：`src/lib/__tests__/real-name-function-contract.test.ts`
    - 实名认证：`src/lib/__tests__/real-name-backend.test.ts`
    - 实名认证：`src/lib/__tests__/real-name-function-backend-contract.test.ts`
    - 设置页：`src/pages/__tests__/Settings.test.tsx`
    - 创建页：`src/pages/__tests__/GroupCreate.test.tsx`
    - 详情页：`src/pages/__tests__/GroupDetail.test.tsx`
  - T10 范围内当前已形成的本地自动化证据：
    - 紧急补位规则：`src/lib/__tests__/group-emergency-fill.test.ts`
    - 推荐排序规则：`src/lib/__tests__/group-recommendation.test.ts`
    - 开局提醒规则：`src/lib/__tests__/group-start-reminder.test.ts`
    - 开局提醒发送编排：`src/lib/__tests__/group-start-reminder-delivery.test.ts`
    - 列表页：`src/pages/__tests__/Index.test.tsx`
    - 首页：`src/pages/__tests__/Home.test.tsx`
    - 详情页：`src/pages/__tests__/GroupDetail.test.tsx`
    - 消息中心：`src/pages/__tests__/Notifications.test.tsx`
  - T11 范围内当前已形成的本地自动化证据：
    - 黑名单规则：`src/lib/__tests__/user-blacklist.test.ts`
    - 黑名单 hook：`src/hooks/__tests__/useBlacklist.test.tsx`
    - 履约档案规则：`src/lib/__tests__/review-insights.test.ts`
    - 资料页：`src/pages/__tests__/Profile.test.tsx`
    - 设置页：`src/pages/__tests__/Settings.test.tsx`
    - 私聊页：`src/pages/__tests__/DirectChat.test.tsx`
    - 好友页：`src/pages/__tests__/Friends.test.tsx`
    - 审核管理页：`src/pages/__tests__/HostRequests.test.tsx`
    - 评价页：`src/pages/__tests__/Review.test.tsx`
- 当前限制：
  - 实名认证当前已完成前后端能力落库与本地自动化验证；
  - 黑名单屏蔽当前已完成本地规则、前台隔离与好友关系清理验证；
  - 标签化评价与履约档案当前已完成本地写库字段接入、履约摘要展示与审核辅助展示验证；
  - 仍缺真实 Supabase 环境部署、RLS / trigger 防绕过、重复拉黑异常提示、多账号真实联调与取消拉黑恢复链路验证证据。

### 5.2 真实联调验收门槛

- 结论：**暂不通过**
- 原因：
  1. 真实 Supabase 环境尚未完成联调验证；
  2. 账号注销的冷静期自动完成、Auth 禁用、通知落库、审计落库、资料脱敏、多端会话一致性等关键项仍属于真实场景测试；
  3. 实名认证的真实写库、审核通过/驳回回写、限制场景口径、敏感字段处理、鉴权链路与服务端强制拦截仍需真实环境验证；
  4. 紧急补位与推荐排序当前已完成前台识别、曝光与智能排序能力补齐，但真实列表排序结果、首页真实定位接入效果、真实通知落库、真实时间推进与点击/申请转化指标仍需联调验证；
  5. 开局提醒当前已完成本地规则、提醒追踪落库结构与消息中心基础交互验证，但真实 migration 部署、Edge Function 定时调度、真实通知写入与多账号收件仍需联调验证；
  6. `auto-send-group-start-reminders` 当前配置为 `verify_jwt = false`，还需在真实部署中验证调度入口保护与非预期调用拦截；
  7. 黑名单屏蔽当前已完成本地自动化验证，但真实 `user_blacklist` migration 部署、好友关系真实清理、私聊写入真实拦截、双向隔离与取消拉黑恢复链路仍需联调验证；
  8. 标签化评价与履约档案当前已完成本地自动化与构建验证，但真实 `reviews.tags` migration 部署、真实标签写库、历史存量兼容、多账号资料页/审核页展示口径仍需联调验证；
  9. 按当前约定，这些 `🟡 需真实场景测试（V2统一执行）` 项将在 **V2 全部开发完成后统一执行**，届时再给出最终联调验收结论。

---

## 6. 后续补充说明

- `T12` 测试用例待对应任务提测后补充；
- 若后续真实联调过程中发现 T9 用例需要新增字段校验、异常兜底或回归项，应继续在本文件下追加，不另起分散文档。
